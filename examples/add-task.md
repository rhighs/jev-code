Create `sum.ts`. Export a function with the signature `add(a: number, b: number): number`. Its body must return `a + b`. Generate a complete valid TypeScript file, including the export, function keyword, braces, and return statement. Then execute this exact Bash command from `.`: `node --experimental-strip-types --input-type=module -e 'import { add } from "./sum.ts"; if (add(2, 3) !== 5) process.exit(1)'`. Inspect and repair errors until the check passes. Do not install dependencies or create other files.
